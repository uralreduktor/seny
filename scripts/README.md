# Скрипты проверки документации

Этот каталог содержит скрипты для автоматической проверки документации проекта.

## Скрипты

### `check_docs_links.py`

Проверяет ссылки в Markdown документации:
- Существование файлов, на которые ссылаются внутренние ссылки
- Существование якорей (заголовков) в документах
- Корректность относительных путей

**Использование**:
```bash
python scripts/check_docs_links.py docs/README.md
python scripts/check_docs_links.py docs/README.md docs/STRUCTURE.md
```

### `check_docs_rules.py`

Проверяет специфичные правила проекта:
- Наличие блока `> 🤖 Для AI-разработчика` в AI Ready документах
- Наличие обязательных разделов
- Структуру документа

**Использование**:
```bash
python scripts/check_docs_rules.py docs/architecture/system_architecture.md
python scripts/check_docs_rules.py docs/modules/tender_management.md
```

## Интеграция с pre-commit

Скрипты автоматически запускаются через pre-commit hooks при каждом коммите. См. `.pre-commit-config.yaml` для конфигурации.

## Ручной запуск всех проверок

```bash
# Через pre-commit
pre-commit run check-markdown-links --all-files
pre-commit run check-docs-rules --all-files

# Напрямую
python scripts/check_docs_links.py docs/**/*.md
python scripts/check_docs_rules.py docs/**/*.md
```
