---
alwaysApply: true
---
## 🎯 Проект в одном предложении
Система для автоматизации ценообразования и подготовки тендерных документов для поставщиков индустриального оборудования с AI-ассистентом, RAG-поиском и OCR чертежей.

## 📚 Технологический стек (строгое соответствие)

### Backend (Python)
- **Python 3.11+** — использовать строгую типизацию, `typing`, `mypy`
- **FastAPI 0.110+** — asyncio, Pydantic v2 для моделей
- **PostgreSQL 16 + pgvector 0.7+** — векторный поиск
- **SQLAlchemy 2.0+** — ORM с async поддержкой
- **Redis 7 + Celery 5** — кеш и фоновые задачи
- **MinIO** — S3-совместимое хранилище файлов

### AI/ML
- **Claude 3.5 Sonnet (основной)** — для AI-ассистента и RAG
- **GPT-4o (резерв)** — fallback для специфических задач
- **OpenAI text-embedding-3-large (3072d)** — эмбеддинги
- **LangChain 0.2+ или LlamaIndex 0.11+** — RAG-оркестрация
- **YOLOv11 + EasyOCR** — OCR и Computer Vision для чертежей

### Frontend (React)
- **React 18 + TypeScript 5.3+** — строгий режим, no `any`
- **Vite** — сборка
- **TanStack Query v5** — кеширование запросов
- **Tailwind CSS** — стилизация
- **shadcn/ui** — компоненты (предпочтительно)

### Инфраструктура
- **Docker + Docker Compose** (development)
- **Kubernetes 1.29+** (production)
- **Grafana + Prometheus + Loki** — мониторинг

### Инструменты разработки
- **OpenAPI Generator 7.0+** — генерация TypeScript клиентов из OpenAPI спецификации FastAPI
- **FastAPI OpenAPI** — автогенерация OpenAPI 3.1 спецификации из Pydantic моделей

---

## 🤖 Правила работы с AI-ассистентом

### ✅ ЧТО ДЕЛАТЬ:

1. **Всегда давайте контекст**: Начинайте запрос с указания модуля (`pricing_kb_ai`, `tender_management`, `docflow`)
2. **Указывайте тип задачи**: `feature`, `bugfix`, `refactor`, `test`, `docs`
3. **Указывайте файлы**: "Используй `backend/app/modules/pricing/routes.py` как пример"
4. **Требуйте соответствие стеку**: "Используй SQLAlchemy async, Pydantic v2"
5. **Запрашивайте безопасность**: "Добавь валидацию входных данных и rate limiting"
6. **Запрашивайте тесты**: "Напиши unit тесты с pytest и coverage >80%"
7. **Для RAG-задач**: "Используй LangChain, pgvector, Claude 3.5, embeddings 3072d"
8. **Для API-клиентов**: "Сгенерируй TypeScript клиент из OpenAPI спецификации через OpenAPI Generator"

### ❌ ЧЕГО НЕ ДЕЛАТЬ:

- Не проси генерировать весь модуль с нуля — лучше по частям
- Не используй устаревшие библиотеки (SQLAlchemy 1.x, Pydantic v1)
- Не забывай про async/await везде, где есть I/O
- Не захардкоживай конфиги — используй Pydantic Settings
- Не игнорируй обработку ошибок и логирование

---

## 🔧 Стиль кода

### Python (FastAPI)
```python
# ✅ ПРАВИЛЬНО:
from typing import Annotated
from fastapi import Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession

async def get_tender(
    tender_id: int,
    db: Annotated[AsyncSession, Depends(get_db)]
) -> Tender:
    result = await db.execute(
        select(Tender).where(Tender.id == tender_id)
    )
    tender = result.scalar_one_or_none()
    if not tender:
        raise HTTPException(status_code=404, detail="Tender not found")
    return tender

# ❌ НЕПРАВИЛЬНО:
def get_tender(tender_id, db):
    return db.query(Tender).filter(Tender.id == tender_id).first()