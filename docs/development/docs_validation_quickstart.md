# Быстрый старт: Автоматическая проверка документации

## 🚀 Установка (5 минут)

### Для проекта с Poetry (рекомендуется)

```bash
# 1. Перейти в backend и добавить pre-commit
cd backend
poetry add --group dev pre-commit

# 2. Обновить lock файл и установить зависимости
poetry lock
poetry install

# 3. Установить hooks
poetry run pre-commit install

# 4. Установить markdownlint (опционально, для ручного запуска)
npm install -g markdownlint-cli
```

> **Примечание**: В Poetry 2.0.0+ используйте `poetry run pre-commit` вместо прямого вызова `pre-commit`.

### Альтернативные варианты

**Через pipx (глобальная установка)**:

```bash
pipx install pre-commit
pre-commit install
```

**Через виртуальное окружение**:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install pre-commit
pre-commit install
```

> ⚠️ Если видите ошибку `externally-managed-environment`, используйте Poetry или pipx.

Готово! Теперь проверки будут запускаться автоматически при каждом коммите.

## ✅ Проверка работы

```bash
# Запустить все проверки вручную
pre-commit run --all-files

# Или только для документации
pre-commit run markdownlint --all-files
pre-commit run check-markdown-links --all-files
pre-commit run check-docs-rules --all-files
```

## 🔧 Что проверяется автоматически

1. **Форматирование Markdown** — исправляется автоматически
2. **Ссылки** — проверка существования файлов и якорей
3. **Правила проекта** — AI Ready блоки, обязательные разделы
4. **Общие проверки** — trailing whitespace, end of file, и т.д.

## 📖 Подробная документация

См. [docs/development/docs_validation.md](./docs_validation.md) для полной документации.
