# Статус Pre-commit Hooks

**Дата обновления**: 2025-11-28

## ✅ Работающие проверки

### Документация

- ✅ **markdownlint** — проверка и автоматическое исправление форматирования Markdown
- ✅ **check-markdown-links** — проверка ссылок в документации (выдаёт предупреждения для TODO файлов)
- ✅ **check-docs-rules** — проверка правил проекта (AI Ready блоки, обязательные разделы)

### Python код

- ✅ **black** — форматирование Python кода
- ✅ **isort** — сортировка импортов

### Общие проверки

- ✅ **trailing-whitespace** — удаление концевых пробелов
- ✅ **end-of-file-fixer** — добавление пустой строки в конце файла
- ✅ **check-yaml** — проверка синтаксиса YAML
- ✅ **check-json** — проверка синтаксиса JSON
- ✅ **check-toml** — проверка синтаксиса TOML
- ✅ **check-added-large-files** — проверка больших файлов
- ✅ **check-merge-conflict** — проверка конфликтов слияния
- ✅ **detect-private-key** — обнаружение приватных ключей

## ⚠️ Временно отключено

### Mypy (проверка типов)

**Статус**: Отключен в `.pre-commit-config.yaml`

**Причина**: Проблема с зависимостью `types-all` — требует несуществующий пакет `types-pkg-resources`

**Решение**:

- Mypy можно запускать вручную: `poetry run -C backend mypy backend/`
- Или использовать только необходимые типы вместо `types-all`

**Влияние**: Минимальное — проверки документации и форматирования работают нормально

## 📊 Статистика проверок

При запуске `pre-commit run --all-files` проверяется:

- Все Markdown файлы в `docs/` и `.cursor/rules/`
- Все Python файлы в `backend/` и `scripts/`
- Конфигурационные файлы (YAML, JSON, TOML)

## 🔧 Настройка

Конфигурация находится в `.pre-commit-config.yaml` в корне проекта.

Для отключения конкретной проверки используйте переменную окружения:

```bash
SKIP=markdownlint git commit
SKIP=mypy git commit
```

---

**Связанные документы**:

- [Автоматическая проверка документации](./docs_validation.md)
- [Решение проблем](./pre_commit_troubleshooting.md)
