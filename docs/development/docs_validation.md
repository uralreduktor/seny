# Автоматическая проверка документации

> **🤖 Для AI-разработчика**: Эта документация описывает систему автоматической проверки документации проекта. Все проверки запускаются автоматически через pre-commit hooks.

## 📋 Обзор

Проект использует автоматическую проверку документации для обеспечения качества и актуальности документации. Проверки выполняются:

- **Автоматически**: При каждом коммите через pre-commit hooks
- **Вручную**: Через команды `pre-commit run`
- **В CI/CD**: При создании Pull Request

## 🔧 Установка

### Требования

- Python 3.11+
- Node.js 20+ (для markdownlint)

### Установка pre-commit

**Вариант 1: Через Poetry (рекомендуется для проекта)**

```bash
cd backend

# Добавить pre-commit как dev зависимость (если ещё не добавлен)
poetry add --group dev pre-commit

# Для Poetry 2.0.0+ используйте:
poetry run pre-commit install

# Или активируйте окружение:
poetry env activate
pre-commit install

# Для Poetry 1.x используйте:
poetry shell
pre-commit install
```

> **Примечание**: В Poetry 2.0.0 команда `poetry shell` удалена. Используйте `poetry run` или `poetry env activate`.

**Вариант 2: Через pipx (для глобальной установки)**

```bash
# Установка pipx (если не установлен)
sudo apt install pipx
pipx ensurepath

# Установка pre-commit через pipx
pipx install pre-commit
pre-commit install
```

**Вариант 3: Через виртуальное окружение Python**

```bash
# Создать виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate на Windows

# Установить pre-commit
pip install pre-commit
pre-commit install
```

**Вариант 4: Через системный пакет (Ubuntu/Debian)**

```bash
sudo apt install pre-commit
pre-commit install
```

> ⚠️ **Примечание**: Если вы видите ошибку `externally-managed-environment`, используйте один из вариантов выше (Poetry, pipx или venv). Не используйте `--break-system-packages`.

### Установка hooks

```bash
# Установка всех hooks
pre-commit install

# Установка hooks для pre-push (опционально)
pre-commit install --hook-type pre-push
```

### Установка markdownlint

```bash
# Через npm (глобально)
npm install -g markdownlint-cli

# Или локально в проект
npm install --save-dev markdownlint-cli
```

## ✅ Типы проверок

### 1. Форматирование Markdown

**Инструмент**: `markdownlint-cli`

**Что проверяет**:

- Форматирование заголовков
- Отступы в списках
- Длина строк (максимум 120 символов, исключая код)
- Пустые строки вокруг заголовков и списков
- Форматирование блоков кода
- И другие стандарты Markdown

**Конфигурация**: `.markdownlint.json`

**Исправление автоматически**:

```bash
# Запуск с автоматическим исправлением
pre-commit run markdownlint --all-files
```

### 2. Проверка ссылок

**Инструмент**: `scripts/check_docs_links.py`

**Что проверяет**:

- Существование файлов, на которые ссылаются внутренние ссылки
- Существование якорей (заголовков) в документах
- Корректность относительных путей

**Пример ошибки**:

```text
❌ docs/README.md: Ссылка '[Архитектура](../architecture/system_architecture.md)' ведёт на несуществующий файл
```

**Запуск вручную**:

```bash
python scripts/check_docs_links.py docs/README.md
```

### 3. Проверка правил проекта

**Инструмент**: `scripts/check_docs_rules.py`

**Что проверяет**:

- Наличие блока `> 🤖 Для AI-разработчика` в AI Ready документах
- Наличие обязательных разделов (Quick Reference, Файловая структура)
- Структуру документа (заголовок первого уровня)

**AI Ready документы** (должны содержать блок для AI):

- `docs/architecture/system_architecture.md`
- `docs/architecture/tech_stack.md`
- `docs/architecture/data_models.md`
- `docs/development/api_docs.md`
- `docs/modules/tender_management.md`
- `docs/modules/docflow.md`
- `docs/development/setup.md`

**Пример предупреждения**:

```text
⚠️  docs/architecture/system_architecture.md: AI Ready документ должен содержать блок '> 🤖 Для AI-разработчика' в начале
```

**Запуск вручную**:

```bash
python scripts/check_docs_rules.py docs/architecture/system_architecture.md
```

### 4. Общие проверки

**Инструмент**: `pre-commit-hooks`

**Что проверяет**:

- Trailing whitespace (концевые пробелы)
- End of file (наличие пустой строки в конце файла)
- YAML/JSON/TOML синтаксис
- Большие файлы (>1MB)
- Конфликты слияния
- Приватные ключи в коде

## 🚀 Использование

### Запуск всех проверок

```bash
# Проверка всех файлов
pre-commit run --all-files

# Проверка только изменённых файлов (по умолчанию)
pre-commit run
```

### Запуск конкретной проверки

```bash
# Только проверка Markdown форматирования
pre-commit run markdownlint --all-files

# Только проверка ссылок
pre-commit run check-markdown-links --all-files

# Только проверка правил проекта
pre-commit run check-docs-rules --all-files
```

## ⚙️ Инженерные чеклисты

### Async clients {#async-clients}

1. **Определи блокирующий SDK** (MinIO, SMTP и т.п.).
   Если библиотека не поддерживает `async`, она должна работать в отдельном потоке или фоне.
2. **Вынеси вызов** в `asyncio.to_thread()` либо Celery-задачу.
   Пример: `backend/app/core/storage.py`.
3. **Делай `await` в сервисах/роутерах** — никаких прямых sync вызовов внутри `async def`.
4. **Логируй ошибки через `loguru`**, а не `print()`.
5. **Обнови документацию/правила**, если добавился новый клиент, и проверь коды линтером.

### Генерация API-клиента фронтенда {#generate-api}

1. Убедись, что backend запущен или есть свежий `openapi.json`.
2. Выполни:

   ```bash
   cd frontend
   npm run generate-api
   ```

3. Проверь, что обновились файлы в `frontend/src/api/generated`.
4. Обнови код, чтобы использовать сгенерированные функции + типы (никаких ручных
   `axios` вызовов в компонентах).
5. Закоммить изменения вместе с актуальной схемой (если менялись эндпоинты).

### Commit checklist {#commit-checklist}

Перед коммитом подтверди:

- [ ] Запущен `pre-commit run --all-files` (или `npm run lint` для фронта).
- [ ] Документация обновлена там, где менялся API/архитектура.
- [ ] Для backend: нет sync вызовов в `async def`, аудит и soft delete соблюдены.
- [ ] Для frontend: используются типы и клиент из `src/api/generated`,
      дизайн соответствует `09-ui-design.mdc`.
- [ ] Сообщение в формате Conventional Commits (`type(scope): subject`).
- [ ] Секреты/конфиденциальные данные не попали в diff.

### Пропуск проверок (не рекомендуется)

```bash
# Пропустить все проверки (только в исключительных случаях!)
git commit --no-verify

# Пропустить конкретную проверку
SKIP=markdownlint git commit
```

## 🔍 Решение проблем

### Mypy hook временно отключен

Mypy hook отключен в `.pre-commit-config.yaml` из-за проблем с зависимостью `types-all` (требует несуществующий `types-pkg-resources`).

**Это не критично**, так как:

- Проверки документации работают нормально
- Проверки форматирования Python (black, isort) работают нормально
- Mypy можно запускать вручную: `poetry run -C backend mypy backend/`

**Подробнее**: См. [pre_commit_troubleshooting.md](./pre_commit_troubleshooting.md)

### Ошибка: "markdownlint: command not found"

**Решение**:

```bash
npm install -g markdownlint-cli
```

### Ошибка: "pre-commit: command not found"

**Решение**:

```bash
pip install pre-commit
# Или
poetry add --group dev pre-commit
```

### Много ошибок форматирования

**Решение**: Запустите автоматическое исправление:

```bash
pre-commit run markdownlint --all-files
```

Большинство ошибок форматирования исправляются автоматически.

### Ссылка на несуществующий файл

**Решение**:

1. Проверьте путь к файлу
2. Убедитесь, что файл существует
3. Исправьте ссылку в документе

### Отсутствует блок для AI-разработчика

**Решение**: Добавьте блок в начало AI Ready документа:

```markdown
> **🤖 Для AI-разработчика**: Описание того, как использовать этот документ.
```

## 📊 Интеграция с CI/CD

Pre-commit hooks автоматически запускаются в CI/CD при создании Pull Request. Проверки выполняются для всех изменённых файлов документации.

### GitHub Actions (пример)

```yaml
name: Check Documentation

on: [pull_request]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - uses: actions/setup-node@v3
        with:
          node-version: "20"
      - run: pip install pre-commit
      - run: npm install -g markdownlint-cli
      - run: pre-commit run --all-files
```

## 📝 Конфигурация

### `.pre-commit-config.yaml`

Основной файл конфигурации pre-commit hooks. Содержит список всех проверок.

### `.markdownlint.json`

Конфигурация правил форматирования Markdown. Настроена под стандарты проекта.

### Скрипты проверки

- `scripts/check_docs_links.py` — проверка ссылок
- `scripts/check_docs_rules.py` — проверка правил проекта

## 🔗 Связанные документы

- [Правила документации](../../.cursor/rules/04-documentation.mdc)
- [Git Workflow](../../.cursor/rules/05-git-workflow.mdc)
- [Аудит документации](../audit_documentation_2025_11_28.md)

---

**Последнее обновление**: 2025-11-28
