# AI/ML компоненты системы

> 🤖 **Для AI-разработчика**:
>
> 1. **Status**: [Out of MVP] - Весь функционал, описанный в этом документе, вынесен за рамки первой версии (MVP).
> 2. **Action**: Не реализовывать этот функционал, пока не будет явного указания.
> 3. **Context**: Данный документ служит справочником для будущих этапов развития системы.
> 4. **MVP Scope**: В MVP используется только базовая инфраструктура (PostgreSQL), но без pgvector и RAG пайплайнов.

## 1. Обзор

Система TenderFlow KB AI планирует использовать современные AI/ML технологии для интеллектуального поиска, анализа чертежей и помощи инженерам в расчётах стоимости.

**Внимание: Реализация данных компонентов отложена (Deferred).**

## 2. Компоненты (Planned)

### 2.1. RAG (Retrieval-Augmented Generation)

**Назначение**: Интеллектуальный поиск похожих расчётов и ответы на вопросы инженеров с использованием контекста из базы знаний.

**Архитектура (Draft)**:

```mermaid
graph LR
    User[User Query] --> Embed[Embedding Model]
    Embed --> VectorDB[(pgvector)]
    VectorDB --> Results[Ranked Docs]
    Results --> LLM[LLM (Claude/GPT)]
    LLM --> Answer[Final Answer]
```

**Технологии**:

- **Embeddings**: OpenAI text-embedding-3-large (3072 dim)
- **Storage**: PostgreSQL + pgvector
- **Framework**: LangChain / LlamaIndex
- **Model**: Claude 3.5 Sonnet

### 2.2. OCR и Computer Vision

**Назначение**: Извлечение данных из чертежей (PDF, DWG).

**Пайплайн (Draft)**:

1. **Detection**: YOLOv11 (поиск таблиц, штампов).
2. **OCR**: EasyOCR / Tesseract (распознавание текста).
3. **Parsing**: Regex + LLM (структурирование).

### 2.3. AI-ассистент

**Назначение**: Чат-бот для проверки гипотез и расчетов.

## 3. Интеграция (Draft Examples)

### Поток данных для AI-поиска (Pseudo-code)

```python
# [Out of MVP] Пример будущей реализации
async def search_similar_calculations(query: str, db: AsyncSession):
    # 1. Генерация embedding
    embedding = await openai_client.embeddings.create(
        model="text-embedding-3-large",
        input=query
    )

    # 2. Векторный поиск
    results = await db.execute(
        select(Calculation)
        .order_by(Calculation.embedding.cosine_distance(embedding))
        .limit(5)
    )
    return results
```

## 4. Ограничения и Риски

1. **RAG**: Галлюцинации при недостатке контекста.
2. **OCR**: Низкая точность на рукописных чертежах.
3. **Cost**: Высокая стоимость токенов при больших объемах документов.

---

**Связанные документы:**

- [Архитектура системы](./system_architecture.md)
- [Технологический стек](./tech_stack.md)
