# Технологический стек

> **🤖 Для AI-разработчика**: Этот документ определяет разрешенные инструменты и версии.
>
> 1. **Strict Versions**: Придерживайся указанных мажорных версий. Не обновляй их самовольно.
> 2. **Minimalism**: Не добавляй новые библиотеки, если задачу можно решить существующими (или стандартной библиотекой).
> 3. **Config**: Все зависимости фиксируются в `pyproject.toml` (Backend) и `package.json` (Frontend).

## 📦 Core Stack Overview

| Область       | Технология | Версия    | Назначение                  |
| ------------- | ---------- | --------- | --------------------------- |
| **Runtime**   | Python     | `3.11+`   | Основной язык Backend       |
| **Runtime**   | Node.js    | `18+`     | Среда Frontend и сборки     |
| **Backend**   | FastAPI    | `0.110+`  | API Framework               |
| **Frontend**  | React      | `18.2+`   | UI Library                  |
| **Database**  | PostgreSQL | `16`      | Основное хранилище          |
| **Vector DB** | pgvector   | `0.7+`    | Векторный поиск (Extension) |
| **Cache**     | Redis      | `7`       | Кеш, брокер очередей        |
| **Storage**   | MinIO      | `RELEASE` | S3-compatible storage       |

---

## 🐍 Backend (Python)

Управление зависимостями через `pip` + `pyproject.toml`.

### Основные библиотеки

| Библиотека    | Версия     | Назначение    | Примечание                                  |
| ------------- | ---------- | ------------- | ------------------------------------------- |
| `fastapi`     | `^0.110.0` | Web Framework | Используй `Annotated` для зависимостей      |
| `pydantic`    | `^2.0.0`   | Validation    | Строго v2 (`model_dump`, `field_validator`) |
| `sqlalchemy`  | `^2.0.0`   | ORM           | Только Async IO (`AsyncSession`)            |
| `alembic`     | `^1.13.0`  | Migrations    | Автогенерация миграций                      |
| `asyncpg`     | `^0.29.0`  | DB Driver     | Асинхронный драйвер PG                      |
| `celery`      | `^5.3.0`   | Task Queue    | Воркеры для OCR и генерации                 |
| `redis`       | `^5.0.0`   | Redis Client  | Async client where possible                 |
| `python-jose` | `^3.3.0`   | JWT Auth      | С `cryptography`                            |
| `passlib`     | `^1.7.4`   | Hashing       | С `bcrypt`                                  |

### AI & Processing

| Библиотека    | Версия    | Назначение        | Примечание                                   |
| ------------- | --------- | ----------------- | -------------------------------------------- |
| `openai`      | `^1.0.0`  | Embeddings API    | -                                            |
| `anthropic`   | `^0.18.0` | Claude LLM API    | -                                            |
| `langchain`   | `^0.2.0`  | RAG Orchestration | _Использовать аккуратно, не переусложнять_   |
| `ultralytics` | `^8.0.0`  | Object Detection  | YOLOv11                                      |
| `easyocr`     | `^1.7.0`  | Text Recognition  | -                                            |

### Dev Tools (Backend)

| Инструмент | Назначение          | Конфигурация                 |
| ---------- | ------------------- | ---------------------------- |
| `pytest`   | Тестирование        | `pytest-asyncio`, `httpx`    |
| `black`    | Форматирование      | Line length: 88              |
| `isort`    | Сортировка импортов | Profile: black               |
| `mypy`     | Проверка типов      | Strict mode                  |
| `ruff`     | Линтер              | (Опционально, замена flake8) |

---

## ⚛️ Frontend (React/TS)

Сборка через `Vite`. Пакетный менеджер `npm`.

### Core Libraries

| Библиотека              | Версия    | Назначение      | Примечание                              |
| ----------------------- | --------- | --------------- | --------------------------------------- |
| `react`                 | `^18.2.0` | UI Framework    | -                                       |
| `typescript`            | `^5.3.0`  | Language        | -                                       |
| `vite`                  | `^5.0.0`  | Bundler         | -                                       |
| `@tanstack/react-query` | `^5.0.0`  | API State Mgmt  | **Обязательно для всех GET запросов**   |
| `react-router-dom`      | `^6.20.0` | Routing         | -                                       |
| `react-hook-form`       | `^7.0.0`  | Forms           | + `zod` для валидации                   |
| `zod`                   | `^3.0.0`  | Schema Validation | -                                     |

### UI & Styles

| Библиотека                | Версия     | Назначение                         |
| ------------------------- | ---------- | ---------------------------------- |
| `tailwindcss`             | `^3.4.0`   | Styling Engine                     |
| `shadcn/ui`               | -          | Component Library (Radix UI based) |
| `lucide-react`            | `^0.300.0` | Icons                              |
| `clsx` / `tailwind-merge` | -          | Утилиты для классов                |

---

## 🏗️ Инфраструктура

### Docker Images (Production)

| Сервис       | Base Image                  | Примечание                   |
| ------------ | --------------------------- | ---------------------------- |
| **Backend**  | `python:3.11-slim-bookworm` | Минимизация размера          |
| **Frontend** | `nginx:alpine`              | Статика + Nginx config       |
| **Postgres** | `ankane/pgvector:v0.7.0`    | Официальный образ с вектором |

### Требования к окружению

- **RAM**: Минимум 4GB (из-за AI моделей и Celery воркеров).
- **GPU**: Желательно для локального OCR (CUDA), но работает и на CPU.
- **Storage**: SSD (важно для MinIO и Vector DB).

---

## ⚠️ Политика обновлений

1. **Security Patches**: Устанавливаются немедленно.
2. **Minor Updates**: Можно обновлять, если не ломает обратную совместимость.
3. **Major Updates**: **ЗАПРЕЩЕНО** без согласования и полного регрессионного тестирования.
   - _Пример_: Не переходить на Pydantic v3 или React 19 без задачи.

---

## 🔗 Полезные ссылки

- [System Architecture](./system_architecture.md) — общая архитектура.
- [Backend Rules](../../.cursor/rules/02-backend-development.mdc) — стандарты кода Python.
- [Frontend Rules](../../.cursor/rules/03-frontend-development.mdc) — стандарты кода React.
