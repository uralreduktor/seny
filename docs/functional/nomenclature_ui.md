# UI: навигация по дереву и динамическая форма номенклатуры

## 1. Навигация по классификатору

- Левый сайдбар на странице `Номенклатура` показывает дерево узлов (`/nomenclature/nodes`),
  сгруппированное по иерархии.
- Клик по узлу:
  - подсвечивает строку дерева;
  - фильтрует таблицу карточек запросом `/nomenclature/cards?node_id={id}`;
  - прокидывает `nodeId` в диалог создания/редактирования для загрузки схемы атрибутов.
- Кнопка «Добавить узел» открывает диалог:
  - родитель подставляется из текущего выбранного узла (или создаётся корневой segment);
  - форма запрашивает `code`, `name`, `node_type`, `description`;
  - по сабмиту вызывается `POST /nomenclature/nodes`, дерево обновляется и сразу выделяет
    созданный узел.
- У каждой строки дерева есть действия:
  - `+` — создание дочернего узла (preselect текущего родителя);
  - `✎` — редактирование (`PATCH /nomenclature/nodes/{id}`) тем же диалогом;
  - `🗑` — архивирование (`DELETE /nomenclature/nodes/{id}`) с подтверждением.

## 2. Поиск и режимы

- Над таблицей расположен ввод текста и селектор `search_mode` (`text`, `semantic`, `combined`).
- Значение пробрасывается в запрос `/nomenclature/cards` и отображается бэдж `score 0.82`
  рядом с кодом карточки.
- При отсутствии выбранного узла кнопка «Создать» показывает подсказку внутри диалога,
  сохранение блокируется.

## 3. Динамическая форма карточки

- Диалог `NomenclatureDialog`:
  - читает схему через `/nomenclature/nodes/{nodeId}/schemas` (последняя версия) и
    отображает группы/поля;
  - при редактировании делает `GET /nomenclature/cards/{id}` для получения `attributes_payload`.
- Компонент `NomenclatureAttributesForm`:
  - поддерживает типы `string/number/enum` и группировку по `x-group`;
  - синхронно обновляет `attributes_payload`, удаляя ключи при пустом значении;
  - выводит служебный блок с текущим JSON для отладки;
  - использует `description` в качестве отображаемого названия поля (если оно заполнено), поэтому
    пользователи видят человеко-понятные подписи вроде «Вариант сборки для параллельных осей по ГОСТ 20373-94».
- Блок «Дополнительные поля» (в правой колонке диалога) позволяет добавить произвольные пары `ключ → значение`, которые
  сохраняются в `tags` конкретной карточки и не зависят от схемы узла.
- Сабмит:
  - `POST /nomenclature/cards` при создании (требуется выбранный узел);
  - `PATCH /nomenclature/cards/{id}` при редактировании.

## 4. Тестирование

- Интеграционные сценарии лежат в
  `frontend/src/components/__tests__/NomenclatureAttributesForm.test.tsx`
  (Testing Library + Vitest). Покрываем генерацию полей, приведение типов и очистку значений.
- Для регрессионной проверки UI достаточно `npm run test` (Vitest) и `npm run build`.

## 5. UX заметки

- Если для узла нет опубликованной схемы, форму атрибутов блокируем предупреждением
  (значения сохраняются как `{}`).
- Атрибуты отображаются в две колонки с единицами измерения, чтобы форма занимала всю ширину
  на широких экранах («как для широких экранов»).

## 6. Редактор схемы узла

- Вкладка работает с итоговым объектом `type: object`, наследование от родителей не отображается.
- Для каждого свойства показаны:
  - чекбокс «Включено»;
  - тип (`string`, `number`, `boolean`, `array`);
  - обязательность (`required`), заголовок, описание, единица измерения, enum, default.
- Тип `array` работает с перечислением значений и отображается на карточке как мультиселект (select
  с возможностью выбрать несколько опций), что удобно для полей вроде «Способ монтажа».
- Если нужно быстро взять поля из готовых пресетов, отметьте их в списке и нажмите кнопку «Заполнить
  полями из выбранных пресетов» — атрибуты добавятся в текущую схему как черновые.
- Значение по умолчанию валидируется по типу (число → `Number`, boolean → переключатель).
- Можно добавлять новые поля (ключ + тип), остальные параметры редактируются в карточке поля.
- Справа отображается JSON-превью; именно этот объект отправляется в
  `/nomenclature/nodes/{id}/schemas`.
- Пример «Новый сегмент»: добавляем `power`, `ratio`, `protection`, `is_explosion_protected`.
  Итоговый JSON:

  ```json
  {
    "type": "object",
    "properties": {
      "power": { "type": "number", "title": "Мощность", "x-unit": "кВт" },
      "ratio": {
        "type": "string",
        "title": "Передаточное число",
        "description": "Например 1:15"
      },
      "protection": {
        "type": "string",
        "title": "Класс защиты",
        "enum": ["IP54", "IP65", "IP67"]
      },
      "is_explosion_protected": {
        "type": "boolean",
        "title": "Взрывозащита",
        "default": false
      }
    },
    "required": ["power"]
  }
  ```

## 7. Редактор пресетов атрибутов

- На странице пресетов теперь тот же визуальный конструктор: поля добавляются кнопкой
  «Добавить поле», доступны тип, описание, единица измерения, enum и default (включая новый `array`).
- JSON Schema собирается автоматически, снизу — `JSON Preview` (read-only). Текстовый ввод больше
  не нужен.
- Поля можно временно отключать или делать обязательными. Для `string` есть список значений через
  запятую, для `number` — единица измерения и проверка числовых дефолтов.
- Перед сохранением выполняется валидация: если default не соответствует типу или нет активных
  полей, форма не отправится.
- После сохранения список пресетов обновляется; опубликованные пресеты по-прежнему меняются через
  `PATCH /nomenclature/presets/{id}` с последующим обновлением схем узлов. Для массивов значения
  задаём через запятую — в карточке они появятся как мультиселект.

## 8. Статус реализации

| Блок                  | Статус     | Комментарий                                                                               |
| --------------------- | ---------- | ----------------------------------------------------------------------------------------- |
| Дерево классификатора | ✅ Готово  | CRUD узлов, фильтрация карточек, подсчёт потомков.                                        |
| Диалог карточки       | ✅ Готово  | Схема из backend, мультиселекты, сохранение типа/категории, пользовательские поля `tags`. |
| Редактор схемы        | ✅ Готово  | Версионирование, diff, публикация, автозаполнение полями из пресетов.                     |
| Пресеты               | ✅ Готово  | Визуальный конструктор, массивы, управление статусами.                                    |
| Поиск                 | ✅ Готово  | Режимы `text/semantic/combined`, отображение `search_confidence`.                         |
| Документация          | ⚙️ Ведётся | Требуется актуализировать OpenAPI и ERD (см. `docs/architecture/nomenclature.md`).        |
