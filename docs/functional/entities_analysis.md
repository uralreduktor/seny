# Критический анализ модели сущностей

## Дата анализа: 2025.11.25

## 🔴 Критические проблемы

### 1. ✅ ИСПРАВЛЕНО: Вычисляемые поля

**Проблема**: Хранение вычисляемых полей нарушает принципы нормализации БД.

**Исправления**:

- ✅ `Position.tender_total_price` — удалено (вычисляется как `quantity × tender_unit_price`)
- ✅ `Nomenclature.usage_count` — помечено как вычисляемое поле (обновляется через триггеры или материализованные представления)
- ✅ `Nomenclature.average_price` — помечено как вычисляемое поле (обновляется через триггеры или материализованные представления)

**Статус**: ✅ Исправлено в entities.md

---

### 2. ✅ РАЗРЕШЕНО: Разные типы цен в Position и Nomenclature

**Уточнение**: Это не дублирование, а разные типы цен на разных этапах:

- `Nomenclature.base_price` — эталонная цена номенклатуры без привязки к тендеру, партии, рискам
- `Position.base_price` — базовая цена позиции конкретного тендера (без скидок, учёта юридических и технических рисков)

**Логика ценообразования**:

1. Nomenclature.base_price → эталонная цена
2. Position.base_price → цена для конкретного тендера (выводится из номенклатуры)
3. Position.tender_unit_price → финальная цена с учётом всех факторов

**Статус**: ✅ Корректно, оставить как есть

---

### 3. ✅ ИСПРАВЛЕНО: Избыточность связей Calculation ↔ Tender ↔ Position

**Проблема**:

- `Calculation.tender_id` — связь с тендером
- `Calculation.position_id` — связь с позицией
- `Position.tender_id` — позиция уже связана с тендером

**Исправление**:

- ✅ Уточнено в описании: `tender_id` обязателен, если нет `position_id`; опциональен, если есть `position_id` (можно получить через позицию)
- ✅ Добавлено примечание о логике получения tender_id через position_id

**Статус**: ✅ Исправлено в entities.md

---

### 4. ✅ РАЗРЕШЕНО: Разные методики на разных этапах

**Уточнение**: Это не дублирование, а разные методики на разных этапах расчёта:

- `Nomenclature.methodology_ids` — методики для расчёта эталонной цены номенклатуры
- `Position.methodology_id` — методика для расчёта цены позиции конкретного тендера (может отличаться от номенклатурной)
- `Calculation.methodology_id` — фактически использованная методика в конкретном расчёте

**Логика расчёта**:

1. Nomenclature → Methodology → базовая цена номенклатуры
2. Position → Methodology → цена позиции (может быть скорректирована для тендера)
3. Calculation → Methodology → финальный расчёт с учётом всех факторов

**Статус**: ✅ Корректно, оставить как есть

---

### 5. ✅ РАЗРЕШЕНО: Формулы и коэффициенты могут быть скорректированы

**Уточнение**: Это не дублирование, а возможность корректировки:

- `Methodology.formulas` и `Methodology.coefficients` — базовые формулы и коэффициенты методики
- `Calculation.formulas` и `Calculation.coefficients` — могут быть скорректированы для конкретного расчёта

**Логика**:

- Методика содержит базовые формулы
- В расчёте могут быть применены корректировки коэффициентов для учёта специфики тендера
- Хранение в Calculation необходимо для фиксации фактически использованных значений

**Статус**: ✅ Корректно, оставить как есть

---

### 6. Нарушение нормализации: JSON вместо связей

**Проблема**: Хранение связей в JSON вместо нормализованных таблиц.

**Примеры**:

- `CommercialProposal.positions` (JSON) — должна быть таблица `commercial_proposal_positions`
- `DocumentPackage.documents` (JSON) — должна быть таблица `document_package_documents`
- `Nomenclature.methodology_ids` (JSON) — уже есть связь многие-ко-многим

**Рекомендация**: Создать промежуточные таблицы для связей многие-ко-многим.

---

### 7. ✅ ИСПРАВЛЕНО: Двунаправленная связь Position ↔ Calculation

**Проблема**:

- `Position.calculation_id` — ссылка на расчёт
- `Calculation.position_id` — ссылка на позицию

**Исправление**:

- ✅ Переименовано `Position.calculation_id` → `Position.current_calculation_id` (для удобства доступа к текущему расчёту)
- ✅ Уточнено, что позиция может иметь множество расчётов, один из которых текущий
- ✅ Основная связь остаётся `Calculation.position_id`

**Статус**: ✅ Исправлено в entities.md

---

### 8. ✅ ИСПРАВЛЕНО: Иерархия category vs subclass vs type

**Проблема**: В Nomenclature есть три похожих поля без ясного назначения.

**Исправление**:

- ✅ Уточнена иерархия классификации:
  - `type` — верхний уровень (например: "редуктор", "мотор-редуктор")
  - `category` — средний уровень (например: "цилиндрический", "конический")
  - `subclass` — нижний уровень (например: "одноступенчатый", "двухступенчатый")

**Статус**: ✅ Исправлено в entities.md

---

### 9. ✅ ИСПРАВЛЕНО: nomenclature_id в Calculation

**Проблема**:

- `Calculation.nomenclature_id` — связь с номенклатурой
- `Calculation.position_id` — связь с позицией
- `Position.nomenclature_id` — позиция уже связана с номенклатурой

**Исправление**:

- ✅ Уточнено в описании: `nomenclature_id` опционален, получается через позицию, если есть `position_id`
- ✅ Оставлен для случаев, когда расчёт не привязан к позиции

**Статус**: ✅ Исправлено в entities.md

---

### 10. ✅ ИСПРАВЛЕНО: Версионность methodology_version в Calculation

**Проблема**:

- `Methodology.version` — версия методики
- `Calculation.methodology_version` — версия использованной методики

**Исправление**:

- ✅ Уточнено назначение: `methodology_version` используется для фиксации версии на момент расчёта
- ✅ Добавлено примечание о логике версионирования (если методика версионируется через отдельные записи)

**Статус**: ✅ Исправлено в entities.md

---

## ⚠️ Потенциальные проблемы

### 11. Статистика в Nomenclature

**Проблема**: `usage_count` и `average_price` — вычисляемые поля, которые нужно обновлять при каждом изменении.

**Рекомендация**:

- Либо убрать и вычислять на лету
- Либо использовать материализованные представления или триггеры для автоматического обновления

---

### 12. Файлы в JSON

**Проблема**:

- `Position.original_files` (JSON)
- `Nomenclature.files` (JSON)

**Вопрос**: Нужна ли отдельная таблица `Files` для нормализации?

**Рекомендация**: Рассмотреть создание таблицы `File` с полями:

- `id`, `entity_type`, `entity_id`, `file_path`, `file_type`, `file_size`, `created_at`

---

### 13. Комментарии и примечания

**Проблема**: Разные сущности имеют разные поля для комментариев:

- `Tender.notes`
- `Position.engineer_comment`

**Рекомендация**: Рассмотреть унификацию или создание отдельной таблицы `Comments`.

---

### 14. ✅ РАЗРЕШЕНО: Версионность позиций

**Уточнение**: `Position.version` — версия названия/описания позиции от заказчика.

**Логика**:

- Одна и та же позиция может иметь разные названия в разных тендерах
- Или заказчик может изменить название/описание в процессе уточнения
- Это версия описания позиции, а не версия пересчёта (версии пересчётов хранятся в Calculation)

**Статус**: ✅ Корректно, уточнить описание в entities.md

---

## ✅ Рекомендации по улучшению

### Приоритет 1 (Критично)

1. ✅ **Убрать вычисляемые поля** (`tender_total_price` удалено, `usage_count`, `average_price` помечены как вычисляемые)
2. ✅ **Разрешить конфликт цен** — уточнено, что это разные типы цен на разных этапах
3. ✅ **Убрать избыточные связи** — уточнена логика получения tender_id через position_id
4. ✅ **Убрать дублирование методики** — уточнено, что это разные методики на разных этапах
5. ⚠️ **Создать нормализованные таблицы** — добавлены примечания о необходимости замены JSON на таблицы связи (требует реализации)

### Приоритет 2 (Важно)

1. ✅ **Убрать дублирование формул/коэффициентов** — уточнено, что это корректировки для конкретного расчёта
2. ✅ **Упростить связь Position ↔ Calculation** — переименовано в `current_calculation_id`, уточнена логика
3. ✅ **Уточнить назначение** `category`, `subclass`, `type` — определена иерархия классификации
4. ✅ **Уточнить `nomenclature_id`** в Calculation — добавлено примечание о получении через Position

### Приоритет 3 (Желательно)

1. ✅ **Уточнить логику версионирования** — уточнено назначение `version` в Position (версия названия/описания)
2. ⚠️ **Рассмотреть создание таблицы File** — требует реализации (добавлено в рекомендации)
3. ⚠️ **Унифицировать комментарии** — требует реализации (добавлено в рекомендации)

---

## 📊 Сводная таблица проблем

| Проблема                      | Тип          | Сущность                            | Приоритет | Статус                       |
| ----------------------------- | ------------ | ----------------------------------- | --------- | ---------------------------- |
| Вычисляемые поля              | Нормализация | Position, Nomenclature              | P1        | ✅ Исправлено                |
| Дублирование цен              | Дублирование | Position, Nomenclature              | P1        | ✅ Разрешено (разные типы)   |
| Избыточные связи              | Избыточность | Calculation                         | P1        | ✅ Исправлено                |
| Дублирование методики         | Дублирование | Position, Calculation               | P1        | ✅ Разрешено (разные этапы)  |
| JSON вместо связей            | Нормализация | CommercialProposal, DocumentPackage | P1        | ⚠️ Требует реализации        |
| Двунаправленная связь         | Избыточность | Position, Calculation               | P2        | ✅ Исправлено                |
| category/subclass/type        | Неясность    | Nomenclature                        | P2        | ✅ Исправлено                |
| Дублирование формул           | Дублирование | Calculation                         | P2        | ✅ Разрешено (корректировки) |
| nomenclature_id в Calculation | Избыточность | Calculation                         | P2        | ✅ Исправлено                |
| Версионность                  | Неясность    | Position, Methodology               | P3        | ✅ Исправлено                |

---

## 🎯 Предлагаемая структура исправлений

### Вариант 1: Минимальные изменения (быстро)

1. Убрать вычисляемые поля
2. Убрать `Calculation.tender_id` (если есть `position_id`)
3. Убрать `Position.methodology_id`
4. Убрать `Position.calculation_id` (оставить только `Calculation.position_id`)
5. Убрать `formulas` и `coefficients` из Calculation

### Вариант 2: Полная нормализация (правильно)

1. Все изменения из Варианта 1
2. Создать таблицы для связей многие-ко-многим
3. Создать таблицу File для нормализации файлов
4. Уточнить иерархию category/subclass/type
5. Реализовать правильное версионирование

---

---

## 📋 Итоговая сводка

### ✅ Исправлено (9 проблем)

1. ✅ Вычисляемые поля — удалено `tender_total_price`, помечены как вычисляемые `usage_count` и `average_price`
2. ✅ Цены — уточнено, что это разные типы цен на разных этапах
3. ✅ Избыточные связи — уточнена логика получения `tender_id` через `position_id`
4. ✅ Методики — уточнено, что это разные методики на разных этапах расчёта
5. ✅ Двунаправленная связь — переименовано в `current_calculation_id`
6. ✅ Иерархия классификации — определена иерархия `type` → `category` → `subclass`
7. ✅ Формулы/коэффициенты — уточнено, что это корректировки для конкретного расчёта
8. ✅ nomenclature_id — уточнено получение через Position
9. ✅ Версионность — уточнено назначение версий в Position и Calculation

### ⚠️ Требует реализации (3 задачи)

1. ⚠️ **Нормализация JSON связей** — создать таблицы `commercial_proposal_positions` и `document_package_documents`
2. ⚠️ **Таблица File** — рассмотреть создание нормализованной таблицы для файлов
3. ⚠️ **Унификация комментариев** — рассмотреть создание отдельной таблицы Comments

### 📊 Статистика

- **Всего проблем выявлено**: 14
- **Исправлено**: 9 (64%)
- **Требует реализации**: 3 (21%)
- **Разрешено как корректные**: 2 (15%)

---

**Дата создания**: 2025.11.25
**Дата обновления**: 2025.11.25 (с учётом уточнений)
**Автор анализа**: AI Assistant
**Статус**: ✅ Большинство проблем исправлено, остались задачи на реализацию
