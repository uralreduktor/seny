# TenderFlow KB AI

Система для автоматизации ценообразования и подготовки тендерных документов для поставщиков индустриального оборудования с AI-ассистентом, RAG-поиском и OCR чертежей.

## 🎯 Описание проекта

TenderFlow KB AI — единая корпоративная платформа для полного цикла участия в тендерных закупках в роли поставщика сложного машиностроительного оборудования (индустриальные редукторы, мотор-редукторы, шестерни, специальные комплектующие).

## 📚 Технологический стек

### Backend
- Python 3.11+ с строгой типизацией
- FastAPI 0.110+ (asyncio, Pydantic v2)
- PostgreSQL 16 + pgvector 0.7+ (векторный поиск)
- SQLAlchemy 2.0+ (async ORM)
- Redis 7 + Celery 5 (кеш и фоновые задачи)
- MinIO (S3-совместимое хранилище файлов)

### AI/ML
- Claude 3.5 Sonnet (основной) / GPT-4o (резерв)
- OpenAI text-embedding-3-large (3072d)
- LangChain 0.2+ / LlamaIndex 0.11+ (RAG)
- YOLOv11 + EasyOCR (OCR чертежей)

### Frontend
- React 18 + TypeScript 5.3+
- Vite (сборка)
- TanStack Query v5
- Tailwind CSS
- shadcn/ui

### Инфраструктура
- Docker + Docker Compose (development)
- Kubernetes 1.29+ (production)
- Grafana + Prometheus + Loki (мониторинг)

## 🏗️ Структура проекта

```
seny/
├── backend/              # FastAPI приложение
│   ├── app/
│   │   ├── modules/      # Модули: pricing_kb_ai, tender_management, docflow
│   │   ├── core/         # Общие компоненты
│   │   └── db/           # Подключение к БД, миграции
│   └── tests/
├── frontend/             # React приложение
│   ├── src/
│   │   ├── api/         # API клиенты (генерируются через OpenAPI Generator)
│   │   ├── components/  # React компоненты
│   │   └── pages/       # Страницы приложения
│   └── public/
├── docs/                 # Документация проекта
├── .cursor/              # Правила для Cursor IDE
└── docker-compose.yml    # Docker Compose конфигурация
```

## 🚀 Быстрый старт

### Требования
- Python 3.11+
- Node.js 18+
- Docker и Docker Compose
- PostgreSQL 16
- Redis 7

### Установка

1. Клонируйте репозиторий:
```bash
git clone git@github.com:uralreduktor/seny.git
cd seny
```

2. Настройте переменные окружения:
```bash
cp backend/.env.example backend/.env
# Отредактируйте backend/.env
```

3. Запустите через Docker Compose:
```bash
docker-compose up -d
```

4. Или локально:
```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
uvicorn app.main:app --reload

# Frontend
cd frontend
npm install
npm run dev
```

## 📖 Документация

- [Техническое задание](docs/project_brief.md)
- [Анализ текущего состояния](docs/analysis_current_state.md)
- [OpenAPI Generator](docs/openapi-generator.md)
- [Правила разработки](.cursor/rules/)

## 🔧 Разработка

### Генерация TypeScript клиентов из OpenAPI

После изменений API на backend:

```bash
cd frontend
npm run generate-api
```

### Правила работы с AI-ассистентом

См. [.cursor/rules/rulles.mdc](.cursor/rules/rulles.mdc)

## 📝 Лицензия

Проприетарное программное обеспечение. Все права защищены.

## 👥 Команда

Ural Reduktor
