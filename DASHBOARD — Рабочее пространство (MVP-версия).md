#  **DASHBOARD — Рабочее пространство (MVP-версия)**

Dashboard — это **центр навигации**, а не место для создания сущностей «в воздухе».
 Из него пользователь **ищет**, **фильтрует**, **открывает**, но не создаёт «осиротевшие» записи.

Создание доступно только в логичных точках:

- Позиция → создаётся внутри Тендера
- Номенклатура → создаётся из Позиции
- Расчёт → создаётся из Номенклатуры

------

# **1. Основные блоки Dashboard**

### **🏷️ Блок 1. Последние открытые документы**

Формат плитками:

- Последние тендеры
- Последние позиции
- Последние номенклатуры
- Последние расчёты

Каждая плитка → быстрый переход в карточку.

**Метаданные:**

- тип документа,
- дата последнего действия,
- пользователь,
- статус (для тендеров: «в работе», «проверка», «подано»).

------

### **🔍 Блок 2. Быстрый поиск по системе**

Единая строка поиска:

Поддержка:

- по названию тендера,
- по названию позиции,
- по номенклатуре,
- по параметрам (мощность, типоразмер...),
- по коду, артикулу,
- по меткам,
- по тексту ТЗ.

Вывод результатов таблично, с фильтрами.

------

### **📚 Блок 3. Ключевые реестры**

Тут — **только просмотр**, без создания:

1. **Реестр тендеров**
2. **Реестр позиций**
3. **Реестр номенклатуры**
4. **Реестр расчётов**

У каждого реестра:

- фильтры,
- сортировка,
- быстрые действия (открыть, дублировать, открыть в новой вкладке).

> **Нет кнопки “Создать” в этих реестрах**, потому что создание доступно из логичных контекстов.
>
> Кнопка “Создать” только для "новый тендер"

------

### **📈 Блок 4. Мини-дашборды и аналитика**

Простые, но полезные графики:

- Кол-во активных тендеров по статусам
- Сколько позиций в работе
- Среднее время подготовки расчёта
- Распределение номенклатуры по типам

Плюс цифры:

- активных тендеров,
- новых позиций за неделю,
- расчётов за месяц.

------

### **🎯 Блок 5. Панель быстрых задач**

Задачи, доступные пользователю как короткие маршруты:

- Открыть последний тендер
- Найти позицию по параметрам
- Найти номенклатуру
- Перейти к моим расчётам

(Но кнопка **создания** только для нового тендера )

------

# **2. Где происходит создание сущностей**

Тут строго по процессу:

------

### **📌 Создание новой позиции**

Только внутри карточки Тендера:

**Тендер → Позиции → ➕ Добавить позицию**

Позиция получает:

- связь с тендером,
- черновое название из ТЗ,
- исходные характеристики,
- прикреплённые файлы.

------

### **📐 Создание номенклатуры**

Только внутри карточки Позиции:

**Позиция → Номенклатура → ➕ Создать номенклатуру**

Или выбрать из уже существующей.

------

### **⚙️ Создание расчёта**

Только из карточки Номенклатуры:

**Номенклатура → Расчёты → ➕ Новый расчёт**

Расчёт подтягивает:

- методику,
- коэффициенты,
- параметры номенклатуры.

------

# **3. Форматы данных (в рамках MVP)**

### Тендер:

- UUID
- Название
- Номер / идентификатор площадки
- Дата окончания
- Статус
- Заказчик
- Сумма / валюта (черновые)
- Список позиций

### Позиция:

- UUID
- Привязка: TenderID
- Название (как в ТЗ)
- Технические характеристики (структурированные + файлы ТЗ/чертежей)
- Привязанная НоменклатураID
- Цена базовая
- Цена на партию
- МетодикаID (опционально)
- Черновые заметки

### Номенклатура:

- UUID
- Название
- Тип / категория
- Технические параметры (структурировано)
- Производитель / стандарт
- Привязка к методикам (список)
- История версий

### Расчёт:

- UUID
- НоменклатураID
- МетодикаID
- Формулы
- Коэффициенты
- Дата расчёта
- Итоговая цена
- Версия расчёта

